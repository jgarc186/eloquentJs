function reverseArray(values) {
	let revese = [];
	for (value of values) {
		revese.unshift(value);
	}
	return revese;
}

function reverseArrayInPlace(values) {
	let revese = [];
	for (value of values) {
		revese.unshift(value);
	}
	return revese;
}